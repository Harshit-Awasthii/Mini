<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-blue-900 p-10">
        <div class="w-full justify-end flex">
            <a class="bg-blue-300 rounded-md px-3 py-2 inline-block" href="/logout">logout</a>
        </div>
        <h4 class="mb-5">You can edit your post ✍️.</h4>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind.....?" class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content %></textarea>
            <input class="px-3 py-2 w-40 text-white bg-zinc-900 block rounded-md mt-2" type="submit" value="Update post">
        </form>
    </div>
</body>
</html>